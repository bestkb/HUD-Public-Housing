---
title: "R Notebook"
output: html_notebook
---

```{r, include=TRUE}
# reading the dataset
library(readr)
df <- read_csv("https://raw.githubusercontent.com/bestkb/HUD-Public-Housing/main/data/locations_inspectionscores_forMeri_Nov.csv")
View(df)
```

```{r, include = TRUE}
# Header for year
HEADER_YEAR <- c('year', 'count', 'mean', 'std', 'min', '25%', '50%', '75%', 'max')

# Header for state
HEADER_STATE <- c('state', 'count', 'mean', 'std', 'min', '25%', '50%', '75%', 'max')

```


```{r, include=TRUE}
# specify the file path
file_path <- 'C:/Users/Farhan/OneDrive/Documents/Github Repositories/HUD-Public-Housing/figures/stats_inspection_score_year.csv'

# Statistic CSV table -- by year
stat_file <- file(file_path, 'w')

# Create the CSV writer
write.table(as.data.frame(t()))
```